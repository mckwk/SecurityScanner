services:
  api:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./user_data:/app/user_data
    environment:
      NMAP_PATH: /usr/bin/nmap
      API_KEY: "${API_KEY:-}"
    cap_add:
      - NET_RAW
      - NET_ADMIN
    restart: unless-stopped